{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tsc: build",
            "type": "shell",
            "command": "npm run build",
            "options": {"cwd": "${workspaceFolder}/buildAndReleaseTask/UseOpenTofuV0"},
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$tsc"],
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}
